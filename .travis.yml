language: python
python:
  - "3.5"
branches:
  only:
  - master
install:
- echo "installing"
services:
  - elasticsearch
script:
- |
  pip install -r requirements.txt
  python -m pytest -m "not integration_elastic_search"
env:
global:

